# ADR — Blue-Green

Status: Accepted

Берём blue-green. Рядом крутятся blue (v1.0.0) и green (v1.1.0), nginx льёт в blue. Прогрелся green — переключаем upstream. Сломалось — обратно.

Canary не берём: в коде нет try/except, даже 10% битого трафика — уже плохо.

Риски: память x2 во время деплоя, и бд (если будет) — следить за миграциями.
